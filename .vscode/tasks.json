{
  "version": "2.0.0",
  "tasks": [
    {
      "label": "Run Claude in Docker",
      "type": "shell",
      "command": "docker run -it --rm -v /mnt/d/code/staged4u:/workspace -w /workspace node:18-bullseye bash -c \"npm install -g @anthropic-ai/claude-code && claude\"",
      "problemMatcher": [],
      "presentation": {
        "reveal": "always",
        "panel": "dedicated"
      }
    }
  ]
}
